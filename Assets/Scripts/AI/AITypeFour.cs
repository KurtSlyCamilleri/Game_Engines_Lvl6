using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AITypeFour : MonoBehaviour
{
    //bypasses obstacles and chase player for a set amount of time


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
